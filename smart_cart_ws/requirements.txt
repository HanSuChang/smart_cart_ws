# ================================================================
# requirements.txt
# Python 의존성 — pip install -r requirements.txt
# ================================================================

# AI / 영상처리
ultralytics>=8.0.0         # YOLOv8
deep-sort-realtime>=1.3.2  # DeepSORT 추적
opencv-python>=4.8.0       # OpenCV
numpy>=1.24.0

# 칼만 필터 (Python 담당이 사용할 경우)
filterpy>=1.4.5

# 시리얼 통신 (OpenCR ↔ RPi4)
pyserial>=3.5

# GUI (GUI 담당이 선택)
# PyQt5>=5.15.0            # GUI 프레임워크 (필요 시 주석 해제)
